import React from 'react';

const Footer = (props) => (
  <footer className="mw7 center">
    <p className="sans-serif f6 tc">&copy; { new Date().getFullYear() } Dan Gayle.</p>
  </footer>
);

export default Footer;
